
    // TODO: Fix to_string.
    /*
    void UI::set_visibility_types(const ObjectAttributes& visibility_types)
    {
        if (is_ready() == false)
        {
            return;
        }

        // Call JavaScript.
        Awesomium::JSArray arguments;

        arguments.Push(Awesomium::WSLit(Convert::to_string(visibility_types).c_str()));

        call("ui_set_visibility_types", arguments);
    }
    */
